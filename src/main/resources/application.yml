spring:
  application:
    name: tanzu-java-web-app
  env: 
    - name: MY_VARIABLE
      value: "ValueFrom: application.yaml"
management:
  endpoints:
    web:
      exposure:
        include: "*"  
  endpoint:
    health:
      show-details: always